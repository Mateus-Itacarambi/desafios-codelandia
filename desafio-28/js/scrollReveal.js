window.sr =ScrollReveal({ reset: true });

sr.reveal('header h1, header p, header button', { duration: 1500});
sr.reveal('#ultimo-jogo, #card-bem-vindo, #ultimas-noticias h2, #card-noticia, #ultimas-noticias button', { duration: 1000});