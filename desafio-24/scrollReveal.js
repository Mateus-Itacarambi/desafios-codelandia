window.sr = ScrollReveal({ reset: true });

sr.reveal('header h1, header .container_botoes, aside p, .trailer', { duration: 2500 });
sr.reveal('header h1', {scale: 0.8});
sr.reveal('header .container_botoes, aside p, .trailer', {delay: 1000});